# MC-CyCOP: 임무중심 사이버 공동상황도 (Mission-Centric Cyber Common Operational Picture)

LLM 기반 국방 사이버 통합운영 관제 상황도. 지리 지도가 아니라 **임무-자산 의존성 그래프**를
좌표계로 사용하고, M&S(what-if) 시뮬레이션을 상황도에 결합한다.

## 이 파일의 역할

세션 시작 시 자동으로 로드되는 상시 지시문이다. 여기에는 **불변에 가까운 규칙**만 적는다.
설계 논거와 상세는 `docs/` 아래에 있고, 아래 목차에서 필요한 것만 열어 읽는다.

> **작업 시작 전 `STATE.md`를 먼저 읽는다.** 데이터 취득 진행 상황과 다음 할 일이 거기 있다.
> 상태가 바뀌면 `STATE.md`를 갱신한다.

- `docs/00-architecture.md` - 계층 아키텍처, 3트랙 구성
- `docs/01-datasets.md` - 공개 데이터셋 목록, 함정, 조달 절차
- `docs/02-research-plan.md` - 논문/과제 계획서
- `docs/03-mission-ontology.md` - L2 임무-자산 온톨로지 스키마 (자체 정의 대상)
- `docs/04-evaluation.md` - 평가 프로토콜, 금지 지표
- `docs/05-data-lifecycle.md` - **데이터셋 취득·파생·삭제 절차. 데이터 작업 전 필독**
- `docs/06-design-workflow.md` - Figma MCP 사용 규칙과 호출 한도
- `docs/07-synthetic-data.md` - 오프라인 합성 트랙. **합성 데이터 사용 한계 포함**
- `docs/99-open-questions.md` - 미확정 사항 (작업 전 반드시 확인)
- `docs/adr/` - 아키텍처 결정 기록. **아래 규칙의 근거는 전부 여기 있다.**

## 확정된 설계 결정 (변경하려면 먼저 논의할 것)

각 항목의 근거는 괄호 안 ADR에 있다. 이 규칙과 충돌하는 구현을 요청받으면
먼저 해당 ADR을 읽고, 규칙을 깨야 한다고 판단되면 코드 대신 ADR 개정안을 제시한다.

1. **좌표계는 임무-자산 의존성 그래프다.** 지리 레이어는 자산의 물리적 위치 표시 용도로만
   쓰고, 기본 뷰가 아니다. (ADR-0001)
2. **외부 IP 지오로케이션 결과는 어떤 판단 로직에도 입력되지 않는다.** 국가 단위 참고값으로
   스키마에 보관만 하고, 스코어·우선순위·자동대응에 쓰지 않는다. (ADR-0002)
3. **상태 산출은 결정론적 엔진이 한다.** 우선순위, 임무 저하도, 색상 등급은 규칙 + 그래프
   알고리즘 + 경량 ML의 몫이다. LLM은 요약, 설명, 상관 가설 생성, 자연어 질의, ATT&CK 매핑
   서술만 담당한다. **LLM 출력이 상태값을 직접 결정하는 경로를 만들지 않는다.** (ADR-0003)
4. **차단·격리 등 대응 실행은 사람 승인이 필수다.** 자동 실행 경로를 코드에 넣지 않는다.
   (ADR-0004)
5. **정규화 스키마는 OCSF로 고정한다.** 위협 인텔은 STIX/TAXII, 기법 라벨은 MITRE ATT&CK.
   (ADR-0005)
6. **파인튜닝하지 않는다. RAG + 프롬프트 + 평가셋으로 간다.** 소형 모델 파인튜닝은 평가셋에서
   RAG의 한계가 실측된 뒤에만 옵션으로 검토한다. (ADR-0006)
7. **온프렘 배치 가능한 모델을 전제로 설계한다.** 모델 어댑터 계층을 두고, 모델 교체가
   프롬프트 템플릿 + 평가셋 교체로 끝나야 한다. 특정 벤더 API에만 있는 기능에 의존하지
   않는다. (ADR-0007)
8. **평가에 accuracy를 쓰지 않는다.** 클래스 불균형이 극단적이다(악성 비율 0.001% 수준).
   PR-AUC, precision@k, 캘리브레이션 지표(ECE, Brier)를 쓴다. (ADR-0008)
9. **모든 LLM 응답은 근거 스팬을 인용한다.** 근거를 붙이지 못하는 응답은 UI에서 "미검증"으로
   표기하고, 인용률 자체를 평가 지표로 삼는다. (ADR-0009)
10. **데이터셋 원본은 임시 자원이다.** 받아서 → 파생물을 뽑고 → 자립성을 확인한 뒤 → 원본을
    지운다. `data/processed/`, `models/`, `eval/results/`는 절대 삭제하지 않는다. (ADR-0010)
11. **합성 데이터로 탐지 성능을 주장하지 않는다.** 악성 비율이 실데이터보다 두 자릿수 높다.
    개발·회귀 테스트·구성 타당성 검사까지만 쓰고, 성능 지표는 실데이터에서만 낸다.
    (`docs/07-synthetic-data.md` 6절)

## 작업 규칙

- **대용량 데이터셋 작업은 플랜 모드로 시작한다.** OpTC는 이벤트 87억 건 규모다. 전체 스캔
  코드를 짜기 전에 항상 샘플(1일치, 1호스트)로 스키마를 확정한다.
- **원본 데이터셋 파일은 절대 수정하지 않는다.** `data/raw/`는 읽기 전용으로 취급하고,
  가공 결과는 `data/interim/`, `data/processed/`에 쓴다.
- **데이터셋 파일을 저장소에 커밋하지 않는다.** 크기와 라이선스 문제가 동시에 걸린다.
  경로는 `configs/datasets.yaml`로 참조한다.
- **파생 스크립트를 짜기 전에 "뽑을 항목 목록"을 먼저 확정한다.** 원본을 지운 뒤
  "그것도 뽑을걸" 하면 수백 GB 재다운로드다. 원본은 단일 순회로 처리한다.
- **취득 즉시 `MANIFEST.json`을 쓴다** (URL, 부분 지정자, 해시, 라이선스, 재배포 가능 여부).
  원본을 지워도 이것은 남는다.
- **삭제는 VERIFY 게이트 통과 후 사람 확인을 거쳐 실행한다.** 자동 정리 스크립트·크론을
  만들지 않는다.
- 파이프라인 코드는 재실행 가능해야 한다. 중간 산출물에 입력 해시와 코드 버전을 기록한다.
- 새 외부 데이터셋이나 라이브러리를 도입하면 `docs/01-datasets.md`에 라이선스와 재배포
  가능 여부를 함께 적는다.
- 설계 수준의 결정을 내렸으면 `docs/adr/`에 ADR을 추가한다. 대화에만 남기지 않는다.

## 인용·수치 취급 주의

`docs/` 안의 수치와 출처는 초기 조사 단계에서 수집된 것이고 **아직 원문 대조 검증이 끝나지
않았다.** 논문이나 제안서에 인용하기 전에 `docs/02-research-plan.md`의 "출처 검증 체크리스트"를
소진해야 한다. 검증되지 않은 수치를 확정 사실처럼 서술하지 않는다.

## 미확정 사항

작업이 이 갈림길에 닿으면 진행하기 전에 확인한다. 상세는 `docs/99-open-questions.md`.

- 대상 망: **국방망(행정/엔터프라이즈 유사)** vs **전술망(대역폭 제약, 전술 C2)** - 미정
- 심볼 체계: MIL-STD-2525 준용 여부 - 미정
- 산출물 성격: 학위논문 / 과제 제안서 / 프로토타입 데모 중 우선순위 - 미정

## 개발 환경

- 플랫폼: Windows 11, PowerShell 5.1. `&&` 체이닝이 안 되므로 `;` 또는 `if ($?) { }`를 쓴다.
- **Python / Node / Java / dotnet 전부 미설치.** `python.exe`는 Microsoft Store 스텁이라
  실행하면 exit 9009. 현재 유일한 런타임은 PowerShell 5.1이다.
- git 미설치. 첫 커밋 전에 git 설치 후 `git init` 필요.
- 최종 구현 언어는 Python 예정. 현재 PowerShell 참조 구현은 포팅 대상이며, 데이터
  아티팩트(JSON/JSONL)는 언어 독립이므로 그대로 유지된다.

### PowerShell 5.1 인코딩 규칙 (어기면 데이터가 조용히 오염된다)

BOM 없는 UTF-8 `.ps1`을 시스템 ANSI 코드페이지로 읽는다. 실제로 두 번 사고가 났다 -
파일 목록 8개 중 7개만 인식, 그리고 한글 라벨이 깨진 채 생성 데이터에 기록.

1. 취득·다운로드 스크립트는 **ASCII만** 쓴다.
2. 한글이 들어가는 스크립트는 **BOM을 붙인다**. `scripts\Add-Bom.ps1` 실행.
   `-Check`는 필요한 파일이 있으면 exit 1이므로 CI에 넣는다.
3. 사람이 읽는 문자열은 가능하면 **JSON 데이터 파일**에 둔다(`-Encoding utf8`로 읽으므로 안전).
4. 콘솔 한글 출력이 필요하면 `[Console]::OutputEncoding = [System.Text.Encoding]::UTF8`.
